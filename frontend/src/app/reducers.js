import { combineReducers } from 'redux';


const main = (state, action) =>
{
    switch (action.type) {
        default:
            return {

            }
    }
}

export const reducers = combineReducers({
    main
});